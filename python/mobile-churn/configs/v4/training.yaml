training_params:
  epochs: 150
  batch_size: 32
  optimizer:
    name: "Adam"
    learning_rate: 0.001
    weight_decay: 0.0005
  loss_function:
    use_pos_weight: false
  scheduler:
    name: "ReduceLROnPlateau"
    patience: 5
    factor: 0.5
  early_stopping:
    patience: 12
    delta: 0.001
