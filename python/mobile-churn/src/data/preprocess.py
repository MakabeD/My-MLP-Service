import pandas as pd


class preprocess():
    def __innit__(self, datasource, dataset_info):
        self.datasource = datasource
        self.dataset_info = dataset_info

    def clering(self, df, dataset_info):
        return
        
